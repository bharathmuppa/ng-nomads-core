import {NormalizeUserRolePipe} from './normalize-user-role.pipe';

describe('NormalizeUserRolePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeUserRolePipe();
    expect(pipe).toBeTruthy();
  });
});
